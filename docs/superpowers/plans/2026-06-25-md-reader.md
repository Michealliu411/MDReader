# MD Reader 实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 构建一个 macOS 轻量 Markdown 阅读器桌面应用,主读 spec 文档,提供大纲导航、清爽排版、阅读进度、文件内搜索。

**Architecture:** Tauri v2 桌面应用。Rust 后端只负责文件读取和进度持久化,前端 React + TS 负责 Markdown 解析(markdown-it)、渲染、大纲、搜索。前后端通过 Tauri `invoke` 通信。

**Tech Stack:** Tauri v2, React 18, TypeScript, markdown-it, highlight.js, Vitest, Rust(rustc/cargo)

**Spec:** `docs/superpowers/specs/2026-06-25-md-reader-design.md`

---

## 文件结构

实现完成后的目录布局(脚手架生成的不重复列出):

```
src/                              # 前端源码(脚手架生成)
├── lib/
│   ├── markdown.ts               # md → {html, headings} 解析
│   ├── tauri-bridge.ts           # 封装 invoke: readFile/saveProgress/loadProgress
│   └── search.ts                 # DOM 搜索:高亮 + 上下跳转
├── components/
│   ├── Sidebar.tsx               # 大纲导航
│   ├── Reader.tsx                # 正文渲染区
│   └── SearchBar.tsx             # 搜索栏
├── hooks/
│   └── useProgress.ts            # 进度读写(防抖保存)
├── App.tsx                       # 顶层状态中枢
└── main.tsx                      # 入口(脚手架生成)

src-tauri/src/
├── main.rs                       # Tauri 命令: read_file / save_progress / load_progress
└── progress.rs                   # progress.json 读写逻辑

src/lib/__tests__/markdown.test.ts
src/lib/__tests__/search.test.ts
src-tauri/src/progress.rs         # 含 #[cfg(test)] 模块
```

---

## Task 0: 环境准备 — 安装 Rust 工具链

**Files:** 无(系统环境)

- [ ] **Step 1: 安装 rustup**

Run:
```bash
curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh -s -- -y
```

- [ ] **Step 2: 激活环境变量并验证**

Run:
```bash
source "$HOME/.cargo/env"
rustc --version
cargo --version
```
Expected: 两个命令都输出版本号(如 `rustc 1.xx.x`)

- [ ] **Step 3: Commit(仅记录环境就绪,无文件变更)**

本任务无代码提交。在后续任务首次 `cargo` 调用前确保 `source "$HOME/.cargo/env"` 已执行。

---

## Task 1: 脚手架 — 创建 Tauri + React + TS 项目

**Files:**
- Create: 项目根目录下所有脚手架文件

- [ ] **Step 1: 用 create-tauri-app 生成项目**

在 `/Users/apple/` 下生成,然后合并进现有项目目录(因为 `/Users/apple/ZCodeProject` 已有 docs/ 和 .git):

Run:
```bash
cd /Users/apple
npm create tauri-app@latest md-reader-scaffold -- --template react-ts --manager npm
```
Expected: 生成 `/Users/apple/md-reader-scaffold/` 目录,含 React + TS + Tauri v2 脚手架。

- [ ] **Step 2: 把脚手架内容移入项目目录**

Run:
```bash
cd /Users/apple/ZCodeProject
# 移动脚手架文件(保留已有的 docs/ 和 .git)
cp -R /Users/apple/md-reader-scaffold/. .
# 合并 .gitignore(脚手架的 + 我们已有的)
```

检查 `docs/superpowers/` 仍然存在(没被覆盖)。

- [ ] **Step 3: 安装前端依赖**

Run:
```bash
cd /Users/apple/ZCodeProject
npm install
```
Expected: 生成 `node_modules/`,无错误。

- [ ] **Step 4: 验证脚手架能启动(需 Rust 已装)**

Run:
```bash
source "$HOME/.cargo/env"
npm run tauri dev
```
Expected: 弹出 Tauri 窗口,显示默认 React 欢迎页。确认后 Ctrl+C 关闭。

- [ ] **Step 5: 清理脚手架默认内容,准备接入**

删除脚手架默认的演示组件,保留 `main.tsx` 入口和最小 `App.tsx`:

`src/App.tsx` 改为:
```tsx
function App() {
  return <div className="app">MD Reader(待实现)</div>;
}

export default App;
```

- [ ] **Step 6: Commit**

```bash
git add -A
git commit -m "chore: scaffold Tauri v2 + React + TS project"
```

---

## Task 2: 安装核心依赖

**Files:**
- Modify: `package.json`, `src-tauri/Cargo.toml`

- [ ] **Step 1: 安装前端运行时依赖**

Run:
```bash
npm install markdown-it highlight.js
npm install -D @types/markdown-it
```

- [ ] **Step 2: 安装前端测试依赖**

Run:
```bash
npm install -D vitest @testing-library/jest-dom jsdom
```

- [ ] **Step 3: 配置 Vitest**

Create `vitest.config.ts`:
```ts
import { defineConfig } from 'vitest/config';
import react from '@vitejs/plugin-react';

export default defineConfig({
  plugins: [react()],
  test: {
    environment: 'jsdom',
    globals: true,
  },
});
```

在 `package.json` 的 `scripts` 中添加:
```json
"test": "vitest run",
"test:watch": "vitest"
```

- [ ] **Step 4: Commit**

```bash
git add -A
git commit -m "chore: add markdown-it, highlight.js, vitest dependencies"
```

---

## Task 3: markdown.ts — Markdown 解析与标题树提取

**Files:**
- Create: `src/lib/markdown.ts`
- Test: `src/lib/__tests__/markdown.test.ts`

- [ ] **Step 1: 写失败测试**

Create `src/lib/__tests__/markdown.test.ts`:
```ts
import { describe, it, expect } from 'vitest';
import { parse } from '../markdown';

describe('parse', () => {
  it('返回 html 和 headings', () => {
    const md = '# 标题一\n\n正文段落\n\n## 子标题\n\n更多正文';
    const { html, headings } = parse(md);

    expect(html).toContain('<h1>标题一</h1>');
    expect(html).toContain('<h2>子标题</h2>');
    expect(html).toContain('<p>正文段落</p>');
    expect(headings).toHaveLength(2);
    expect(headings[0]).toEqual({
      id: '标题一',
      level: 1,
      text: '标题一',
    });
    expect(headings[1]).toEqual({
      id: '子标题',
      level: 2,
      text: '子标题',
    });
  });

  it('只提取 H1-H4,忽略 H5/H6', () => {
    const md = '# H1\n##### H5\n###### H6';
    const { headings } = parse(md);
    expect(headings).toHaveLength(1);
    expect(headings[0].level).toBe(1);
  });

  it('代码块正常渲染', () => {
    const md = '```js\nconst x = 1;\n```';
    const { html } = parse(md);
    expect(html).toContain('<code');
    expect(html).toContain('const x = 1');
  });

  it('空文件不报错,返回空结果', () => {
    const { html, headings } = parse('');
    expect(html).toBe('');
    expect(headings).toHaveLength(0);
  });

  it('纯文本不报错', () => {
    const { html, headings } = parse('只是一段纯文本');
    expect(html).toContain('只是一段纯文本');
    expect(headings).toHaveLength(0);
  });
});
```

- [ ] **Step 2: 运行测试,确认失败**

Run: `npx vitest run src/lib/__tests__/markdown.test.ts`
Expected: FAIL — `Cannot find module '../markdown'`

- [ ] **Step 3: 实现 markdown.ts**

Create `src/lib/markdown.ts`:
```ts
import MarkdownIt from 'markdown-it';
import hljs from 'highlight.js';

export interface Heading {
  id: string;
  level: number;
  text: string;
}

export interface ParseResult {
  html: string;
  headings: Heading[];
}

const md = new MarkdownIt({
  html: false,
  linkify: true,
  highlight(str: string, lang: string): string {
    if (lang && hljs.getLanguage(lang)) {
      try {
        return `<pre class="hljs"><code>${
          hljs.highlight(str, { language: lang }).value
        }</code></pre>`;
      } catch {
        // fall through
      }
    }
    return `<pre class="hljs"><code>${md.utils.escapeHtml(str)}</code></pre>`;
  },
});

export function parse(source: string): ParseResult {
  if (!source) {
    return { html: '', headings: [] };
  }

  const tokens = md.parse(source, {});
  const headings: Heading[] = [];

  for (const token of tokens) {
    if (token.type === 'heading_open') {
      const level = parseInt(token.tag.slice(1), 10);
      if (level >= 1 && level <= 4) {
        // 下一个 inline token 包含标题文本
        const inlineToken = tokens[tokens.indexOf(token) + 1];
        const text = inlineToken?.content ?? '';
        headings.push({ id: text, level, text });
      }
    }
  }

  const html = md.render(source);
  return { html, headings };
}
```

- [ ] **Step 4: 运行测试,确认通过**

Run: `npx vitest run src/lib/__tests__/markdown.test.ts`
Expected: PASS — 5 个测试全过

- [ ] **Step 5: Commit**

```bash
git add src/lib/markdown.ts src/lib/__tests__/markdown.test.ts
git commit -m "feat: add markdown parser with heading extraction"
```

---

## Task 4: tauri-bridge.ts — 封装 Tauri invoke 调用

**Files:**
- Create: `src/lib/tauri-bridge.ts`

- [ ] **Step 1: 实现 tauri-bridge.ts**

Create `src/lib/tauri-bridge.ts`:
```ts
import { invoke } from '@tauri-apps/api/core';

export async function readFile(path: string): Promise<string> {
  return invoke<string>('read_file', { path });
}

export interface ProgressEntry {
  headingId: string;
  updatedAt: number;
}

export async function saveProgress(
  path: string,
  headingId: string
): Promise<void> {
  await invoke('save_progress', { path, headingId });
}

export async function loadProgress(path: string): Promise<ProgressEntry | null> {
  return invoke<ProgressEntry | null>('load_progress', { path });
}
```

- [ ] **Step 2: Commit**

```bash
git add src/lib/tauri-bridge.ts
git commit -m "feat: add tauri-bridge for invoke wrappers"
```

说明:此模块依赖 Tauri 运行时,单测需 mock invoke,价值低,按 spec 靠手动验收覆盖。

---

## Task 5: progress.rs — Rust 端进度持久化(含单元测试)

**Files:**
- Create: `src-tauri/src/progress.rs`
- Modify: `src-tauri/src/main.rs`

- [ ] **Step 1: 写失败测试(内联在 progress.rs)**

Create `src-tauri/src/progress.rs`:
```rust
use serde::{Deserialize, Serialize};
use std::collections::HashMap;
use std::fs;
use std::path::{Path, PathBuf};

#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct ProgressEntry {
    pub heading_id: String,
    pub updated_at: i64,
}

pub type ProgressMap = HashMap<String, ProgressEntry>;

fn progress_file_path(dir: &Path) -> PathBuf {
    dir.join("progress.json")
}

pub fn load(dir: &Path) -> ProgressMap {
    let path = progress_file_path(dir);
    match fs::read_to_string(&path) {
        Ok(content) => serde_json::from_str(&content).unwrap_or_default(),
        Err(_) => ProgressMap::new(),
    }
}

pub fn get(map: &ProgressMap, file_path: &str) -> Option<ProgressEntry> {
    map.get(file_path).cloned()
}

pub fn set(
    dir: &Path,
    file_path: &str,
    heading_id: String,
    timestamp: i64,
) -> Result<(), String> {
    let mut map = load(dir);
    map.insert(
        file_path.to_string(),
        ProgressEntry {
            heading_id,
            updated_at: timestamp,
        },
    );
    let json =
        serde_json::to_string_pretty(&map).map_err(|e| e.to_string())?;
    fs::create_dir_all(dir).map_err(|e| e.to_string())?;
    fs::write(progress_file_path(dir), json).map_err(|e| e.to_string())?;
    Ok(())
}

#[cfg(test)]
mod tests {
    use super::*;
    use std::env;

    fn temp_dir() -> PathBuf {
        let mut d = env::temp_dir();
        d.push(format!(
            "mdreader-test-{}-{}",
            std::process::id(),
            std::time::SystemTime::now()
                .duration_since(std::time::UNIX_EPOCH)
                .unwrap()
                .as_nanos()
        ));
        fs::create_dir_all(&d).unwrap();
        d
    }

    #[test]
    fn load_empty_dir_returns_empty_map() {
        let dir = temp_dir();
        let map = load(&dir);
        assert!(map.is_empty());
    }

    #[test]
    fn set_then_load_roundtrip() {
        let dir = temp_dir();
        set(&dir, "/a/spec.md", "概述".to_string(), 1000).unwrap();
        let map = load(&dir);
        let entry = map.get("/a/spec.md").unwrap();
        assert_eq!(entry.heading_id, "概述");
        assert_eq!(entry.updated_at, 1000);
    }

    #[test]
    fn get_missing_returns_none() {
        let map = ProgressMap::new();
        assert!(get(&map, "/none.md").is_none());
    }

    #[test]
    fn set_overwrites_existing() {
        let dir = temp_dir();
        set(&dir, "/a.md", "旧标题".to_string(), 1).unwrap();
        set(&dir, "/a.md", "新标题".to_string(), 2).unwrap();
        let map = load(&dir);
        let entry = map.get("/a.md").unwrap();
        assert_eq!(entry.heading_id, "新标题");
        assert_eq!(entry.updated_at, 2);
    }

    #[test]
    fn corrupted_json_returns_empty() {
        let dir = temp_dir();
        fs::write(progress_file_path(&dir), "{bad json").unwrap();
        let map = load(&dir);
        assert!(map.is_empty());
    }
}
```

- [ ] **Step 2: 运行测试,确认通过**

Run:
```bash
cd src-tauri
cargo test progress
```
Expected: PASS — 5 个测试全过

- [ ] **Step 3: Commit**

```bash
cd /Users/apple/ZCodeProject
git add src-tauri/src/progress.rs
git commit -m "feat: add progress persistence module with tests"
```

---

## Task 6: main.rs — Tauri 命令(read_file / save_progress / load_progress)

**Files:**
- Modify: `src-tauri/src/main.rs`
- Modify: `src-tauri/Cargo.toml`(确认 serde_json 依赖)

- [ ] **Step 1: 确认 Cargo.toml 依赖**

检查 `src-tauri/Cargo.toml` 的 `[dependencies]` 含:
```toml
serde = { version = "1", features = ["derive"] }
serde_json = "1"
tauri = { version = "2", features = [] }
```
缺则补上。

- [ ] **Step 2: 实现 main.rs 命令**

Replace `src-tauri/src/main.rs` 内容为:
```rust
mod progress;

use std::fs;
use std::path::Path;
use tauri::{AppHandle, Manager};

#[tauri::command]
fn read_file(path: String) -> Result<String, String> {
    if !Path::new(&path).exists() {
        return Err(format!("文件不存在: {}", path));
    }
    let bytes = fs::read(&path).map_err(|e| format!("读取失败: {}", e))?;
    // UTF-8 检测
    match String::from_utf8(bytes) {
        Ok(content) => Ok(content),
        Err(_) => Err("文件编码不支持,请转为 UTF-8".to_string()),
    }
}

#[tauri::command]
fn save_progress(
    app: AppHandle,
    path: String,
    heading_id: String,
) -> Result<(), String> {
    let data_dir = app
        .path()
        .app_data_dir()
        .map_err(|e| e.to_string())?;
    let timestamp = chrono::Utc::now().timestamp();
    progress::set(&data_dir, &path, heading_id, timestamp)
}

#[tauri::command]
fn load_progress(app: AppHandle, path: String) -> Result<Option<progress::ProgressEntry>, String> {
    let data_dir = app
        .path()
        .app_data_dir()
        .map_err(|e| e.to_string())?;
    let map = progress::load(&data_dir);
    Ok(progress::get(&map, &path))
}

fn main() {
    tauri::Builder::default()
        .invoke_handler(tauri::generate_handler![
            read_file,
            save_progress,
            load_progress
        ])
        .run(tauri::generate_context!())
        .expect("error while running tauri application");
}
```

- [ ] **Step 3: 添加 chrono 依赖**

在 `src-tauri/Cargo.toml` 的 `[dependencies]` 加:
```toml
chrono = "0.4"
```

- [ ] **Step 4: 编译验证**

Run:
```bash
cd src-tauri
cargo build
```
Expected: 编译成功(可能有 warning,无 error)

- [ ] **Step 5: Commit**

```bash
cd /Users/apple/ZCodeProject
git add src-tauri/src/main.rs src-tauri/Cargo.toml
git commit -m "feat: add Tauri commands read_file/save_progress/load_progress"
```

---

## Task 7: search.ts — 文件内搜索

**Files:**
- Create: `src/lib/search.ts`
- Test: `src/lib/__tests__/search.test.ts`

- [ ] **Step 1: 写失败测试**

Create `src/lib/__tests__/search.test.ts`:
```ts
import { describe, it, expect } from 'vitest';
import { findMatches } from '../search';

describe('findMatches', () => {
  it('找到所有匹配位置', () => {
    const matches = findMatches('ab ab ab', 'ab', false);
    expect(matches).toEqual([0, 3, 6]);
  });

  it('无匹配返回空数组', () => {
    const matches = findMatches('hello', 'xyz', false);
    expect(matches).toEqual([]);
  });

  it('大小写敏感开', () => {
    const matches = findMatches('Hello hello', 'Hello', true);
    expect(matches).toEqual([0]);
  });

  it('大小写敏感关(默认)', () => {
    const matches = findMatches('Hello hello', 'hello', false);
    expect(matches).toEqual([0, 6]);
  });

  it('空关键词返回空', () => {
    const matches = findMatches('some text', '', false);
    expect(matches).toEqual([]);
  });

  it('next/prev 循环跳转', () => {
    const matches = [0, 3, 6];
    // 当前在第 2 个(index 1),next → 第 3 个
    expect(nextIndex(matches, 1)).toBe(2);
    // 在最后一个,next → 第 1 个(循环)
    expect(nextIndex(matches, 2)).toBe(0);
    // 在第一个,prev → 最后一个(循环)
    expect(prevIndex(matches, 0)).toBe(2);
  });
});
```

- [ ] **Step 2: 运行测试,确认失败**

Run: `npx vitest run src/lib/__tests__/search.test.ts`
Expected: FAIL — `Cannot find module '../search'`

- [ ] **Step 3: 实现 search.ts**

Create `src/lib/search.ts`:
```ts
export function findMatches(
  text: string,
  keyword: string,
  caseSensitive: boolean
): number[] {
  if (!keyword) return [];

  const hay = caseSensitive ? text : text.toLowerCase();
  const needle = caseSensitive ? keyword : keyword.toLowerCase();
  const matches: number[] = [];
  let idx = hay.indexOf(needle);
  while (idx !== -1) {
    matches.push(idx);
    idx = hay.indexOf(needle, idx + needle.length);
  }
  return matches;
}

export function nextIndex(matches: number[], current: number): number {
  if (matches.length === 0) return -1;
  const next = current + 1;
  return next >= matches.length ? 0 : next;
}

export function prevIndex(matches: number[], current: number): number {
  if (matches.length === 0) return -1;
  const prev = current - 1;
  return prev < 0 ? matches.length - 1 : prev;
}
```

- [ ] **Step 4: 运行测试,确认通过**

Run: `npx vitest run src/lib/__tests__/search.test.ts`
Expected: PASS — 6 个测试全过

- [ ] **Step 5: Commit**

```bash
git add src/lib/search.ts src/lib/__tests__/search.test.ts
git commit -m "feat: add in-file search with match navigation"
```

---

## Task 8: Sidebar.tsx — 大纲导航组件

**Files:**
- Create: `src/components/Sidebar.tsx`

- [ ] **Step 1: 实现 Sidebar**

Create `src/components/Sidebar.tsx`:
```tsx
import { Heading } from '../lib/markdown';

interface SidebarProps {
  headings: Heading[];
  activeId: string | null;
  onJump: (id: string) => void;
}

export function Sidebar({ headings, activeId, onJump }: SidebarProps) {
  if (headings.length === 0) {
    return (
      <aside className="sidebar">
        <p className="sidebar-empty">无大纲</p>
      </aside>
    );
  }

  return (
    <aside className="sidebar">
      <nav className="outline">
        {headings.map((h) => (
          <a
            key={h.id}
            className={`outline-item level-${h.level}${
              h.id === activeId ? ' active' : ''
            }`}
            onClick={() => onJump(h.id)}
          >
            {h.text}
          </a>
        ))}
      </nav>
    </aside>
  );
}
```

- [ ] **Step 2: Commit**

```bash
git add src/components/Sidebar.tsx
git commit -m "feat: add Sidebar outline navigation component"
```

---

## Task 9: Reader.tsx — 正文渲染区

**Files:**
- Create: `src/components/Reader.tsx`

- [ ] **Step 1: 实现 Reader**

Create `src/components/Reader.tsx`:
```tsx
interface ReaderProps {
  html: string;
  error: string | null;
}

export function Reader({ html, error }: ReaderProps) {
  if (error) {
    return (
      <main className="reader">
        <div className="reader-error">{error}</div>
      </main>
    );
  }

  if (!html) {
    return (
      <main className="reader">
        <div className="reader-empty">打开一个 Markdown 文件开始阅读</div>
      </main>
    );
  }

  return (
    <main
      className="reader"
      // html 来自本地 markdown-it 解析,非用户输入的外部内容
      dangerouslySetInnerHTML={{ __html: html }}
    />
  );
}
```

- [ ] **Step 2: Commit**

```bash
git add src/components/Reader.tsx
git commit -m "feat: add Reader content area with error/empty states"
```

---

## Task 10: SearchBar.tsx — 搜索栏组件

**Files:**
- Create: `src/components/SearchBar.tsx`

- [ ] **Step 1: 实现 SearchBar**

Create `src/components/SearchBar.tsx`:
```tsx
import { useState, useEffect, useRef } from 'react';

interface SearchBarProps {
  visible: boolean;
  onSearch: (keyword: string, caseSensitive: boolean) => void;
  onNext: () => void;
  onPrev: () => void;
  onClose: () => void;
  matchCount: number;
  currentMatch: number;
}

export function SearchBar({
  visible,
  onSearch,
  onNext,
  onPrev,
  onClose,
  matchCount,
  currentMatch,
}: SearchBarProps) {
  const [keyword, setKeyword] = useState('');
  const [caseSensitive, setCaseSensitive] = useState(false);
  const inputRef = useRef<HTMLInputElement>(null);

  useEffect(() => {
    if (visible) inputRef.current?.focus();
  }, [visible]);

  useEffect(() => {
    onSearch(keyword, caseSensitive);
  }, [keyword, caseSensitive, onSearch]);

  if (!visible) return null;

  return (
    <div className="search-bar">
      <input
        ref={inputRef}
        type="text"
        value={keyword}
        onChange={(e) => setKeyword(e.target.value)}
        placeholder="搜索…"
      />
      <button onClick={() => setCaseSensitive((c) => !c)}>
        {caseSensitive ? 'Aa' : 'aa'}
      </button>
      <span className="search-count">
        {matchCount > 0 ? `${currentMatch + 1}/${matchCount}` : '0/0'}
      </span>
      <button onClick={onPrev}>↑</button>
      <button onClick={onNext}>↓</button>
      <button onClick={onClose}>✕</button>
    </div>
  );
}
```

- [ ] **Step 2: Commit**

```bash
git add src/components/SearchBar.tsx
git commit -m "feat: add SearchBar component"
```

---

## Task 11: useProgress.ts — 进度读写 hook(防抖保存)

**Files:**
- Create: `src/hooks/useProgress.ts`

- [ ] **Step 1: 实现 useProgress**

Create `src/hooks/useProgress.ts`:
```ts
import { useCallback, useEffect, useRef, useState } from 'react';
import { loadProgress, saveProgress } from '../lib/tauri-bridge';

export function useProgress(filePath: string | null) {
  const [restoreId, setRestoreId] = useState<string | null>(null);
  const [activeId, setActiveId] = useState<string | null>(null);
  const timerRef = useRef<ReturnType<typeof setTimeout> | null>(null);

  // 打开文件时恢复进度
  useEffect(() => {
    if (!filePath) {
      setRestoreId(null);
      return;
    }
    let cancelled = false;
    loadProgress(filePath)
      .then((entry) => {
        if (!cancelled && entry) setRestoreId(entry.headingId);
      })
      .catch(() => {
        // 静默失败:首次使用或文件损坏,从头开始
      });
    return () => {
      cancelled = true;
    };
  }, [filePath]);

  // 滚动停顿 1.5s 后保存(防抖)
  const recordHeading = useCallback(
    (id: string) => {
      setActiveId(id);
      if (!filePath) return;
      if (timerRef.current) clearTimeout(timerRef.current);
      timerRef.current = setTimeout(() => {
        saveProgress(filePath, id).catch(() => {
          // 静默失败:进度没存上不打断阅读
        });
      }, 1500);
    },
    [filePath]
  );

  return { restoreId, activeId, recordHeading };
}
```

- [ ] **Step 2: Commit**

```bash
git add src/hooks/useProgress.ts
git commit -m "feat: add useProgress hook with debounced save"
```

---

## Task 12: App.tsx — 顶层状态中枢 + 样式

**Files:**
- Modify: `src/App.tsx`
- Create: `src/styles.css`

- [ ] **Step 1: 实现 App.tsx**

Replace `src/App.tsx`:
```tsx
import { useCallback, useEffect, useRef, useState } from 'react';
import { open } from '@tauri-apps/plugin-dialog';
import { listen } from '@tauri-apps/api/event';
import { parse } from './lib/markdown';
import { readFile } from './lib/tauri-bridge';
import { findMatches, nextIndex, prevIndex } from './lib/search';
import { Sidebar } from './components/Sidebar';
import { Reader } from './components/Reader';
import { SearchBar } from './components/SearchBar';
import { useProgress } from './hooks/useProgress';
import './styles.css';

interface LoadedDoc {
  path: string;
  html: string;
  headings: ReturnType<typeof parse>['headings'];
  text: string;
}

function App() {
  const [doc, setDoc] = useState<LoadedDoc | null>(null);
  const [error, setError] = useState<string | null>(null);
  const [searchVisible, setSearchVisible] = useState(false);
  const [keyword, setKeyword] = useState('');
  const [caseSensitive, setCaseSensitive] = useState(false);
  const [currentMatch, setCurrentMatch] = useState(0);
  const readerRef = useRef<HTMLElement>(null);

  const { restoreId, activeId, recordHeading } = useProgress(doc?.path ?? null);

  const openFile = useCallback(async (path: string) => {
    try {
      const text = await readFile(path);
      const { html, headings } = parse(text);
      setError(null);
      setDoc({ path, html, headings, text });
    } catch (e) {
      setError(String(e));
      setDoc(null);
    }
  }, []);

  // 菜单"打开" → 调系统文件对话框
  const handleOpen = useCallback(async () => {
    const selected = await open({
      filters: [{ name: 'Markdown', extensions: ['md', 'markdown'] }],
    });
    if (typeof selected === 'string') await openFile(selected);
  }, [openFile]);

  // 拖入文件
  useEffect(() => {
    const unlisten = listen<{ paths: string[] }>('tauri://file-drop', (e) => {
      const path = e.payload.paths[0];
      if (path) openFile(path);
    });
    return () => {
      unlisten.then((fn) => fn());
    };
  }, [openFile]);

  // Ctrl/Cmd+F 搜索
  useEffect(() => {
    const handler = (e: KeyboardEvent) => {
      if ((e.metaKey || e.ctrlKey) && e.key === 'f') {
        e.preventDefault();
        setSearchVisible(true);
      }
      if (e.key === 'Escape') setSearchVisible(false);
    };
    window.addEventListener('keydown', handler);
    return () => window.removeEventListener('keydown', handler);
  }, []);

  // 搜索匹配
  const matches = doc
    ? findMatches(doc.text, keyword, caseSensitive)
    : [];

  const handleSearch = useCallback(
    (kw: string, cs: boolean) => {
      setKeyword(kw);
      setCaseSensitive(cs);
      setCurrentMatch(0);
    },
    []
  );

  const handleNext = useCallback(() => {
    setCurrentMatch((c) => nextIndex(matches, c));
  }, [matches]);

  const handlePrev = useCallback(() => {
    setCurrentMatch((c) => prevIndex(matches, c));
  }, [matches]);

  // 滚动监听:记录当前可见标题
  useEffect(() => {
    if (!doc || !readerRef.current) return;
    const reader = readerRef.current;
    const handler = () => {
      const headings = reader.querySelectorAll('h1, h2, h3, h4');
      let visible: Element | null = null;
      for (const h of headings) {
        if (h.getBoundingClientRect().top >= 0) {
          visible = h;
          break;
        }
      }
      if (visible && visible.textContent) {
        recordHeading(visible.textContent);
      }
    };
    reader.addEventListener('scroll', handler);
    return () => reader.removeEventListener('scroll', handler);
  }, [doc, recordHeading]);

  // 恢复进度:滚动到上次标题
  useEffect(() => {
    if (restoreId && readerRef.current && doc) {
      const target = Array.from(
        readerRef.current.querySelectorAll('h1, h2, h3, h4')
      ).find((h) => h.textContent === restoreId);
      target?.scrollIntoView();
    }
  }, [restoreId, doc]);

  const handleJump = useCallback((id: string) => {
    if (!readerRef.current) return;
    const target = Array.from(
      readerRef.current.querySelectorAll('h1, h2, h3, h4')
    ).find((h) => h.textContent === id);
    target?.scrollIntoView({ behavior: 'smooth' });
  }, []);

  return (
    <div className="app">
      <div className="toolbar">
        <button onClick={handleOpen}>打开</button>
      </div>
      <SearchBar
        visible={searchVisible}
        onSearch={handleSearch}
        onNext={handleNext}
        onPrev={handlePrev}
        onClose={() => setSearchVisible(false)}
        matchCount={matches.length}
        currentMatch={currentMatch}
      />
      <div className="main">
        <Sidebar
          headings={doc?.headings ?? []}
          activeId={activeId}
          onJump={handleJump}
        />
        <Reader ref={readerRef} html={doc?.html ?? ''} error={error} />
      </div>
    </div>
  );
}

export default App;
```

注意:`Reader` 需转发 ref。更新 `src/components/Reader.tsx` 用 `forwardRef`:

```tsx
import { forwardRef } from 'react';

interface ReaderProps {
  html: string;
  error: string | null;
}

export const Reader = forwardRef<HTMLElement, ReaderProps>(
  ({ html, error }, ref) => {
    if (error) {
      return (
        <main className="reader" ref={ref}>
          <div className="reader-error">{error}</div>
        </main>
      );
    }
    if (!html) {
      return (
        <main className="reader" ref={ref}>
          <div className="reader-empty">打开一个 Markdown 文件开始阅读</div>
        </main>
      );
    }
    return (
      <main
        className="reader"
        ref={ref}
        dangerouslySetInnerHTML={{ __html: html }}
      />
    );
  }
);

Reader.displayName = 'Reader';
```

- [ ] **Step 2: 写样式 styles.css**

Create `src/styles.css`:
```css
* { box-sizing: border-box; margin: 0; padding: 0; }

.app {
  display: flex;
  flex-direction: column;
  height: 100vh;
  font-family: -apple-system, "Helvetica Neue", sans-serif;
}

.toolbar {
  padding: 6px 12px;
  border-bottom: 1px solid #e0e0e0;
  background: #fafafa;
}

.main {
  display: flex;
  flex: 1;
  overflow: hidden;
}

.sidebar {
  width: 240px;
  border-right: 1px solid #e0e0e0;
  overflow-y: auto;
  padding: 12px;
  background: #fafafa;
}

.sidebar-empty { color: #999; font-size: 13px; }

.outline-item {
  display: block;
  cursor: pointer;
  padding: 4px 8px;
  font-size: 13px;
  color: #444;
  border-radius: 4px;
  text-decoration: none;
}
.outline-item:hover { background: #ececec; }
.outline-item.active { background: #d6e9ff; color: #0066cc; }
.outline-item.level-2 { padding-left: 20px; }
.outline-item.level-3 { padding-left: 32px; }
.outline-item.level-4 { padding-left: 44px; }

.reader {
  flex: 1;
  overflow-y: auto;
  padding: 32px 48px;
  line-height: 1.7;
  color: #222;
}
.reader-empty, .reader-error {
  color: #999;
  text-align: center;
  margin-top: 40vh;
}
.reader-error { color: #c00; }

.reader h1 { font-size: 1.8em; margin: 0.8em 0 0.4em; }
.reader h2 { font-size: 1.4em; margin: 0.8em 0 0.4em; }
.reader h3 { font-size: 1.2em; margin: 0.8em 0 0.4em; }
.reader h4 { font-size: 1.05em; margin: 0.8em 0 0.4em; }
.reader p { margin: 0.6em 0; }
.reader pre.hljs {
  background: #f6f8fa;
  padding: 12px;
  border-radius: 6px;
  overflow-x: auto;
  font-size: 13px;
}
.reader code { font-family: "SF Mono", Menlo, monospace; }
.reader ul, .reader ol { margin: 0.6em 0; padding-left: 1.6em; }

.search-bar {
  display: flex;
  gap: 4px;
  padding: 6px 12px;
  border-bottom: 1px solid #e0e0e0;
  background: #fff;
  align-items: center;
}
.search-bar input { flex: 1; padding: 4px 8px; }
.search-count { font-size: 12px; color: #666; min-width: 50px; }
```

- [ ] **Step 3: 安装 dialog 插件**

Run:
```bash
npm install @tauri-apps/plugin-dialog
```

在 `src-tauri/Cargo.toml` 加:
```toml
tauri-plugin-dialog = "2"
```

在 `src-tauri/src/main.rs` 的 `tauri::Builder` 链上加:
```rust
.plugin(tauri_plugin_dialog::init())
```
(放在 `.invoke_handler(...)` 之前)

- [ ] **Step 4: Commit**

```bash
git add -A
git commit -m "feat: wire up App with open/drag/search/progress + styles"
```

---

## Task 13: 手动验收 + 修复

**Files:** 视问题而定

- [ ] **Step 1: 启动应用**

Run:
```bash
source "$HOME/.cargo/env"
npm run tauri dev
```

- [ ] **Step 2: 逐条过验收清单**

对照 spec 的验收清单:
- [ ] 拖入 .md 能正确显示
- [ ] 大纲点击能跳到对应标题
- [ ] 关闭重开,定位到上次标题
- [ ] Ctrl+F 搜索,高亮+上下跳转正常
- [ ] 打开不存在的文件,显示错误提示不崩溃
- [ ] 打开空文件,显示引导文字

- [ ] **Step 3: 修复发现的问题**

记录并修复验收中发现的问题,逐个 commit。

- [ ] **Step 4: 最终 commit**

```bash
git add -A
git commit -m "fix: address manual QA findings"
```

---

## Self-Review 记录

**1. Spec coverage(对照 spec 逐项):**
- MVP 功能 1 打开文件 → Task 6(read_file)+ Task 12(打开对话框 + 拖入)✅
- MVP 功能 2 大纲导航 → Task 3(headings)+ Task 8(Sidebar)+ Task 12(跳转)✅
- MVP 功能 3 清爽排版 → Task 3(markdown-it)+ Task 12(styles.css)✅
- MVP 功能 4 阅读进度 → Task 5(progress.rs)+ Task 6(命令)+ Task 11(useProgress)+ Task 12(滚动监听/恢复)✅
- MVP 功能 5 文件内搜索 → Task 7(search.ts)+ Task 10(SearchBar)+ Task 12(快捷键/状态)✅
- 错误处理:文件读取失败(Task 6)、空文件(Task 9)、进度损坏(Task 5 corrupted_json 测试)、进度写失败静默(Task 11 catch)✅
- 测试策略:markdown.ts(Task 3)、search.ts(Task 7)、progress.rs(Task 5)、手动验收(Task 13)✅

**2. Placeholder scan:** 无 TBD/TODO。所有步骤含完整代码。✅

**3. Type consistency:**
- `Heading { id, level, text }` — Task 3 定义,Task 8/12 使用,一致 ✅
- `ProgressEntry { headingId, updatedAt }`(TS)/ `{ heading_id, updated_at }`(Rust)— Tauri 自动 camelCase 转换,Task 4/5/6 一致 ✅
- `findMatches(text, keyword, caseSensitive) → number[]` — Task 7 定义,Task 12 使用,一致 ✅
- `nextIndex/prevIndex(matches, current)` — Task 7 定义,Task 12 使用,一致 ✅

**4. 修正项(自审中发现并已修复):**
- Task 12 中 Reader 需要转发 ref(App 用 readerRef 滚动监听),已在 Task 12 Step 1 补充 forwardRef 版本 ✅
